import { ReadyDirective } from './ready.directive';

describe('ReadyDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadyDirective();
    expect(directive).toBeTruthy();
  });
});
